<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title HarmansList | Report Post Form
        style
            include reportpost.css
    body

    div#report-wrapper
        h1 Report This Post
        p Found a post suspicious? Report it here with your details.
        form(method="POST" action="/user/post/report/" + postID)
            input#reason(type="textfield" name="reason" placeholder="Reason")
            br
            input#message(rows="2" type="textfield" name="reasondetails" placeholder="Message")
            br
            input#submit(type="submit" value="Submit Report")

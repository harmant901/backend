<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title HarmansList | Post Created
        style
            include style.css
    body

    h1 Your report has successfully been submitted
    p Click the back button to go back to the panel!

    // variables

    - var usernamer = username;
    form(method="GET" action="/user/panel/" + usernamer)
        input(type="submit" value="Back")